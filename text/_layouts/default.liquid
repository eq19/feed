{%- assign target = site.target_repository | split: '/' -%}
{%- if target[0] == 'eq19' -%}
	{%- if target[1] contains 'github.io' -%}
		{%- include templates/leads/index.html -%}
	{%- else -%}
		{%- include templates/leads/{{ target | last }}.html -%}
	{%- endif -%}
{%- endif -%}

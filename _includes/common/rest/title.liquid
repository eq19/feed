{%- comment -%}
eQuantum | Prime Identity* - Mapping the Quantum Way acrross Prime Identity 👉 150 projects
Theory of Everything* - An attemp to discover the Final Theory* | eQuantum 👈 37 page per project
{%- endcomment -%}

{%- case page.url -%}
    {%- when "/" -%}
        {%- assign title = site.title
            | append: " | "
            | append: docs.latest_release.name 
            | default: page.title
            | append: " - "
            | append: site.description | truncate: 110 -%}
    {%- when "/search.html" -%}
        {%- assign title = __.search | default: "Search"
            | append: " · "
            | append: site.title | truncate: 110 -%}
    {%- when "/404.html" -%}
        {%- assign title = "404"
            | append: " · "
            | append: site.title | truncate: 110 -%}
    {%- else -%}
        {%- assign title = page.title | default: page.name
            | append: " - "
            | append: docs.project_tagline
            | append: " | "
            | append: site.title | truncate: 110 -%}
{%- endcase -%}
